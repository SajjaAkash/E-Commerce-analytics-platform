name: ecommerce_analytics
version: 1.0.0
config-version: 2

profile: ecommerce_analytics

model-paths: ["models"]
seed-paths: ["seeds"]

models:
  ecommerce_analytics:
    staging:
      +materialized: view
      +tags: ["daily"]
    marts:
      +materialized: table
      +tags: ["daily"]
